# ════════════════════════════════════════════════════════════════
# Swipeszter — Production Environment
# Másolat: cp .env.prod .env.prod.local és azt töltsd ki!
# A .env.prod.local legyen gitignore-ban (secrets benne vannak)
# ════════════════════════════════════════════════════════════════

# ─── Domain ──────────────────────────────────────────────────────
DOMAIN=swipeszter.hu              # A te domainedet írd ide

# ─── Laravel APP_KEY ─────────────────────────────────────────────
# Generálás: docker run --rm php:8.4-alpine php -r "echo 'base64:'.base64_encode(random_bytes(32)).PHP_EOL;"
APP_KEY=base64:VÁLTOZTASD_MEG_EZT_EGY_32_BÁJTOS_RANDOM_KULCSRA==

# ─── Adatbázis ───────────────────────────────────────────────────
DB_DATABASE=swipeszter
DB_USERNAME=swipeszter
DB_PASSWORD=ERŐS_DB_JELSZÓ_IDE
DB_ROOT_PASSWORD=ERŐS_ROOT_JELSZÓ_IDE

# ─── Reverb (WebSocket) ──────────────────────────────────────────
# APP_ID: tetszőleges szám, egyedi azonosító
# APP_KEY: tetszőleges string (kb. 20 karakter)
# APP_SECRET: tetszőleges titkos string (kb. 20 karakter)
# Generálás: openssl rand -hex 16
REVERB_APP_ID=100001
REVERB_APP_KEY=REVERB_KULCS_IDE
REVERB_APP_SECRET=REVERB_TITKOS_IDE

# ════════════════════════════════════════════════════════════════
# Az alábbiak általában nem kell változtatni
# ════════════════════════════════════════════════════════════════

# ─── MySQL service-ek közötti beállítások (docker-compose.prod.yml-ben van)
# DB_HOST=mysql (konténer neve, nem kell ide)

# ─── Vite build args (docker-compose.prod.yml olvassa) ───────────
# Ezek NEM kell külön megadni — a DOMAIN és REVERB_APP_KEY-ből automatikusan jönnek
# VITE_REVERB_HOST     = ${DOMAIN}
# VITE_REVERB_PORT     = 443
# VITE_REVERB_SCHEME   = https
# VITE_REVERB_APP_KEY  = ${REVERB_APP_KEY}
